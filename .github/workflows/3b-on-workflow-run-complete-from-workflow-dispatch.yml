name: '3b'

on:
  workflow_run:
    workflows: ['2']
    types: [completed]

jobs:
  post-dispatch:
    runs-on: ubuntu-latest
    env:
      EVENT: ${{ github.event }}
    steps:
      - run: echo $EVENT