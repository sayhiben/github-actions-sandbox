on:
  workflow_run:
    workflows:
      - 1-on-push.yml
    types:
      - completed

jobs:
  hello-world:
    runs-on: ubuntu-latest
    env:
      EVENT: ${{ github.event }}
    steps:
      - run: echo $EVENT